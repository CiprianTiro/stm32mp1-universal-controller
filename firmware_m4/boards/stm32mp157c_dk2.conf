# Matches Zephyr's own openamp_rsc_table sample for this board - the reset
# hook conflicts with the boot state Linux/remoteproc has already put the
# M4 in by the time OpenAMP takes over.
CONFIG_SOC_RESET_HOOK=n
