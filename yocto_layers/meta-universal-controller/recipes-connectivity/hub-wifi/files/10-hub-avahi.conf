# Drop-in for avahi-daemon.service (issue #36): start through
# hub-avahi-start, which uses backend_daemon's one-interface config when
# there is one (see that script for why). The empty ExecStart= clears the
# stock command first.
[Service]
ExecStart=
ExecStart=/usr/libexec/hub-avahi-start
